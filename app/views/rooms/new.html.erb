<% content_for :title, "New Channel" %>

<div class="flex flex-col h-full">
  <header class="h-14 border-b border-border px-6 flex items-center flex-shrink-0">
    <h1 class="text-lg font-semibold text-primary">Create a Channel</h1>
  </header>

  <div class="flex-1 overflow-y-auto p-6">
    <div class="max-w-lg">
      <%= render "form", room: @room, voice_channels: @voice_channels %>
    </div>
  </div>
</div>
